name: em-project
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pandas>=2.0
  - numpy>=1.24
  - matplotlib>=3.8
  - seaborn>=0.13
  - scipy>=1.11
  - scikit-learn>=1.4
  - pip
  - pip:
      # Only if you later add any pip-only libs; leave empty for now
      # - some-extra-package
